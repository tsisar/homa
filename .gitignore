# ── Go ───────────────────────────────────────────────────────────
# Compiled binary (this module builds to ./agent) and other build output
/agent
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binaries and coverage / profiling output
*.test
*.out
*.prof

# Go workspace files (kept local for a single-module repo)
go.work
go.work.sum

# ── Project ──────────────────────────────────────────────────────
# Audio generated by local playback / tests
*.wav

# Parked Ukrainian TTS — recreate with scripts/setup.sh
.venv/
voices/*.onnx
voices/*.onnx.json

# ── Editor / OS / local agent config ─────────────────────────────
/.idea/
*.iml
.vscode/
.DS_Store
/.claude/
/CLAUDE.md
